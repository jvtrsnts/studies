# String in Java

- String é um objeto imutavel em Java
- Métodos que modificam uma String apenas retornam uma nova String
- Métodos estaticos se referem a classe em si, e não a um objeto
  - Não necessita instanciar um classe(objeto) para chamar o método
